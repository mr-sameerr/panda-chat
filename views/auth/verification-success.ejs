<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panda Chat | Email Verification</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="https://bootstrapmade.com/demo/templates/NiceAdmin/assets/img/logo.png">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <!-- Custom styles -->
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Roboto', sans-serif;
        }

        .logo {
            max-width: 100px;
            margin-bottom: 10px;
        }

        .card-container {
            margin-top: 50px;
            /* Adjust the margin top as needed to move the card upwards */
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .check-icon {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background-color: #2acc15;
            color: #ffffff;
            line-height: 95px;
            font-size: 50px;
        }

        .times-icon {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background-color: #fa3c3c;
            color: #ffffff;
            line-height: 95px;
            font-size: 50px;
        }

        .btn-continue {
            width: 200px;
            /* Adjust the width as needed */
            border-radius: 4px;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            background-color: #007bff;
            color: #ffffff;
            border: none;
            transition: background-color 0.3s ease;
        }

        .btn-continue:hover {
            background-color: #0056b3;
        }

        .text-expired {
            font-size: 18px;
            color: #dc3545;
            margin-bottom: 20px;
        }

        .text-verify {
            font-size: 18px;
            color: #2acc15;
            margin-bottom: 20px;
        }

        .text-continue {
            font-size: 16px;
            color: #007bff;
        }

        .min-vh-80 {
            min-height: 80vh;
        }

        .p-5 {
            padding: 6rem !important;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-80 "> <!-- Added card-container class -->
            <div class=""></div>
            <div class="col-md-8"> <!-- Adjust column size as needed -->
                <div class="text-center">
                    <div class="website-info">
                        <img src="https://bootstrapmade.com/demo/templates/NiceAdmin/assets/img/logo.png"
                            alt="Panda Chat Logo" class="logo title-logo">
                    </div>
                </div>
                <!-- <%= data.message %> -->
                <div class="card p-5 text-center">
                    <% if(data.success){ %>
                        <h2 class="mb-4">Email Verified</h2>
                        <div class="check-icon mx-auto mb-4">
                            <i class="fa fa-check" aria-hidden="true"></i>
                        </div>
                        <p class="mb-4 text-verify"><%= data.message %></p>
                    <% } else { %>
                        <h2 class="mb-4">Email Verification Failed</h2>
                        <div class="times-icon mx-auto mb-4">
                            <i class="fa fa-times" aria-hidden="true"></i>
                        </div>
                        <p class="text-expired"><%= data.message %></p>
                    <% } %>
                    <div class="text-center">
                        <a href="/login" class="btn btn-continue">Continue to website</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>